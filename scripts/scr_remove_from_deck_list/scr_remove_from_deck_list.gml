function scr_remove_from_deck_list(argument0) {
	var deck_position =argument0;
	with(obj_editor_select_list.deckList[deck_position]){
				cardNum = 42069;
			
			}

	/*for (i = deck_position; i < editor.cardsInDeck; i++){
		selector.deckList[i].cardNum = selector.deckList[i+1].cardNum;
		}
		with(selector.deckList[editor.cardsInDeck]){
		instance_destroy();
		}

/* end scr_remove_from_deck_list */
}
